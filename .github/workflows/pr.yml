name: pr-checks

on:
  pull_request:

permissions:
  contents: read

jobs:
  required-checks:
    name: PR required checks
    runs-on: ubuntu-latest
    steps:
      - name: Explain required workflow
        run: |
          echo "Pull requests into main must pass the ci workflow."
          echo "Required stages: format, lint, tests, build."
